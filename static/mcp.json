{
  "name": "SigmaGrid Fair Value Signals",
  "version": "1.0.0",
  "description": "Institutional fair value and alpha signals API for synthetic-equity perpetuals (SPY-PERP, QQQ-PERP, TSLA-PERP, etc.). 19 endpoints across 5 pricing tiers. API live.",
  "author": "SigmaGrid",
  "homepage": "https://sigmagrid.app",
  "base_url": "https://api.sigmagrid.app",
  "openapi_spec": "https://api.sigmagrid.app/openapi.yaml",
  "status": {
    "current": "live",
    "note": "API is live. MCP tool support coming soon — skeleton exists (src/mcp/server.py) but list_tools() returns [] and no tools are functional yet."
  },
  "mcp_status": "coming_soon",
  "tools": [],
  "tools_note": "MCP tools are not yet functional. The MCP server exists as a skeleton but list_tools() returns an empty list. Use the OpenAPI spec or direct HTTP calls instead.",
  "endpoints": {
    "free": [
      { "path": "/healthz", "method": "GET", "description": "Health check" },
      { "path": "/v1/validate", "method": "GET", "description": "Service validation + supported assets" },
      { "path": "/api/discovery", "method": "GET", "description": "Bot-discoverable API info" },
      { "path": "/openapi.yaml", "method": "GET", "description": "OpenAPI 3.0 spec" },
      { "path": "/api/registry-services.json", "method": "GET", "description": "Service manifest" }
    ],
    "free_teaser": [
      { "path": "/v1/signals/{ticker}", "method": "GET", "description": "Regime + event-risk labels (no numeric data). Includes gated_endpoints map." }
    ],
    "single_signal_0_02_usdc": [
      { "path": "/v1/fair-value/{ticker}", "method": "GET", "description": "Fair value, confidence, per-venue premium bps" },
      { "path": "/v1/premium/{ticker}", "method": "GET", "description": "Cross-venue mispricing" },
      { "path": "/v1/spread/{ticker}", "method": "GET", "description": "Cross-venue spread + arbitrage flag" },
      { "path": "/v1/funding/{ticker}", "method": "GET", "description": "Funding rates, z-scores (Hyperliquid only)" },
      { "path": "/v1/regime-basic/{ticker}", "method": "GET", "description": "Risk regime + VIX context" },
      { "path": "/v1/regime/{ticker}", "method": "GET", "description": "Regime classification with confidence" },
      { "path": "/v1/event-risk/{ticker}", "method": "GET", "description": "Earnings event-risk" },
      { "path": "/v1/events/{ticker}", "method": "GET", "description": "Event sensitivity" },
      { "path": "/v1/arbitrage/{ticker}", "method": "GET", "description": "Arbitrage detection" }
    ],
    "rich_data_0_03_usdc": [
      { "path": "/v1/historical/{ticker}", "method": "GET", "description": "Historical time-series" },
      { "path": "/v1/alpha-snapshot/{ticker}", "method": "GET", "description": "Full AlphaSnapshot" }
    ],
    "bulk_batch_0_05_usdc": [
      { "path": "/v1/snapshot", "method": "GET", "description": "Multi-ticker snapshot" },
      { "path": "/v1/alpha-snapshot/batch", "method": "GET", "description": "Batch AlphaSnapshot" }
    ],
    "deprecated": [
      { "path": "/v1/drift/{ticker}", "method": "GET", "status": "410 Gone", "description": "Use /v1/alpha-snapshot/{ticker}" }
    ]
  },
  "pricing": {
    "model": "per-request",
    "mechanism": "x402 micropayments on Base L2",
    "network": "Base (Chain ID: 8453)",
    "currency": "USDC",
    "tiers": {
      "free": "0 USDC",
      "single_signal": "0.02 USDC",
      "rich_data": "0.03 USDC",
      "bulk_batch": "0.05 USDC"
    }
  },
  "payment_info": {
    "protocol": "x402",
    "network": "base",
    "chain_id": 8453,
    "currency": "USDC",
    "usdc_contract": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
    "payment_flow": "HTTP 402 → Agent pays on Base → Retry with tx_hash → Get data"
  },
  "testing": {
    "mode": "live",
    "free_endpoints": ["/healthz", "/v1/validate", "/v1/signals/{ticker}"],
    "description": "API is live. Free teaser at /v1/signals/{ticker}. Paid endpoints require x402."
  }
}
